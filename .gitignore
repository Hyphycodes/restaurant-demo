node_modules/
.next/
out/
build/
dist/

# Env — never commit real values. Only .env.example is tracked.
.env
.env.*
!.env.example

# Original-quality media masters live outside the served directory.
/media-originals/

.DS_Store
*.pem
npm-debug.log*
yarn-error.log*
.vercel
*.tsbuildinfo
next-env.d.ts
# Test-coverage output, at the repository root only. Unanchored, this also
# swallowed src/app/staff/schedule/coverage — the shift-coverage screen — which
# is a genuinely hard bug to spot, because the files are simply never staged.
/coverage/

# Local development content database (see docs/ADR-001-ADMIN-BACKEND.md).
# Never used in production; seeded from src/content/.
.casa-aurelia-local/

# Photos uploaded through the admin in local development.
/public/media/uploads/

# Local dev server log
dev.log

.next-dev/

# React Email preview build output
.email-previews/
.react-email/
